# Configuração do Worker (site estático)
# Servindo a pasta dist/ gerada pelo Vite como assets

name = "gestaodma"
main = "worker.js"
compatibility_date = "2024-11-01"

[assets]
directory = "./dist"
binding = "ASSETS"
not_found_handling = "single-page-application"
